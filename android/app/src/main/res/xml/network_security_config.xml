<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plaintext HTTP only for the local dev backend (Android emulator's
  host alias, plus a physical device on the same LAN). Everything else still
  requires HTTPS. Update the LAN entry if your machine's IP changes, and
  remove this file once ApiService.baseUrl points at a real HTTPS API.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
